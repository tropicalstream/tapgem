.gradle/
build/
app/build/
local.properties

# API key — NEVER commit it. It is pushed to the device over adb (see README).
gemini_api_key.txt
*.key
.DS_Store
*.iml
.idea/
captures/
.externalNativeBuild/
.cxx/
.kotlin/

# Audio dropped in for testing the player is personal, not repo content — except the one
# sample track in samples/ (generated with Suno, shipped so a fresh clone has something to play).
*.mp3
*.m4a
*.flac
*.wav
!samples/*.mp3
