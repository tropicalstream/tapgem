<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- The glasses have no radio of their own; this is only ever used to read the paired
         phone's carrier and signal over HFP for the status strip. No scanning, no pairing. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Google Maps "Your location" / directions from here inside a map window
         (only google.com origins are ever granted geolocation). -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Media the user asks for by name ("open my vacation video"). Scoped
         storage covers Pictures/Movies/Music; PDFs/EPUBs/models outside those
         need All-files access (granted once via adb appops, see README). -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <application
        android:name=".TapGemApp"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TapGem"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Required for the RayNeo launcher to list the app. NO ar_mode
             meta-data: it restricts rendering to the left lens only. -->
        <meta-data
            android:name="com.rayneo.mercury.app"
            android:value="true" />

        <!-- Split MAIN+LAUNCHER / MAIN+AR_APP filters — the combined form
             was never proven to launch on device; the split form is. -->
        <activity
            android:name=".MainActivity"
            android:configChanges="density|orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|keyboard|navigation"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTask"
            android:resizeableActivity="false"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="stateAlwaysHidden|adjustNothing"
            android:theme="@style/Theme.TapGem">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.rayneo.intent.category.AR_APP" />
            </intent-filter>
        </activity>

        <!-- Foreground service that keeps the Gemini Live AudioRecord alive. -->
        <service
            android:name=".core.session.TapGemForegroundService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- adb-pushable API key (cold-app path; the running app also has a
             runtime receiver so the plain implicit broadcast works). Guarded
             by DUMP, which only the adb shell and the system hold — no other
             app on the glasses can set the key. -->
        <receiver
            android:name=".core.store.ApiKeyBroadcastReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="com.tapgem.app.SET_API_KEY" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
